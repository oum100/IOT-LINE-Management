# TODO

- Implement device key redundancy on firmware side: NVS primary + backup, with version + CRC.
- Implement short-lived portal recovery token flow (5-10 min) for last-resort key re-issue.
- Next session: extend register flow to return/attach deviceKey for first provisioning.
